#include <stdio.h>
#include <math.h>

int main()
{
   int num;
   int chois;
   
   
   printf ("entre number\n");
   scanf("%d",&num);
   if(num > 0)
        printf("number est positef");
    else if (num < 0)
       printf("number est nugative");
     else 
        printf("nombre est égal à zéro");
   
  
}
